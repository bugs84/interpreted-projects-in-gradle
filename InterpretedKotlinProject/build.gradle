import cz.vondr.gradle.HelloKotlin

group 'cz.vondr.gradle'
version '1.0-SNAPSHOT'

// Dependencies for compilation are in "buildSrc/build.gradle" !

task helloKotlin {
    doLast {
        def hello = new HelloKotlin()
        println hello.hello
    }
}